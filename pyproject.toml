[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "coding-agent-workspace"
version = "0.1.0"
description = "CLI workspace for coordinating coding agents (Python port)"
requires-python = ">=3.9"
dependencies = []

[project.scripts]
coding-agent-workspace = "workspace_cli.cli:main"

[tool.setuptools]
packages = ["workspace_cli"]
