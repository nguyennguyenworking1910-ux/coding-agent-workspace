# Scheduler Agent Configuration

# Google Calendar settings
GOOGLE_CALENDAR_ID=primary
CALENDAR_TIMEZONE=Asia/Ho_Chi_Minh

# Optional: Override model
SCHEDULER_MODEL=claude-opus-5

# Optional: Max scheduling steps
SCHEDULER_MAX_STEPS=10
